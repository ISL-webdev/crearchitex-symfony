{% extends 'base.html.twig' %}
{% block body %}
    <div class="subHeader">
        <div class="wrap">
            <h1>Ajouter un Projet</h1>
        </div>
    </div>
    <div class="wrap">

        <div class="Form">
            {{ form_start(form) }}
            {{ form_row(form.titre) }}
            {{ form_row(form.description) }}
            {{ form_row(form.projet_categorie_id) }}
            {#<label>Catégorie#}
                {#<select>#}
                    {#{% for cat in categories %}#}
                        {#<option value="{{ cat.projetcategorieid }}">{{ cat.categorie }}</option>#}
                    {#{% endfor %}#}
                {#</select>#}
            {#</label>#}
            {{ form_row(form.photo) }}
            {{ form_row(form.submit) }}
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
