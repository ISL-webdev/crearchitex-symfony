{% extends 'base.html.twig' %}

{% block body %}
    <div class="subHeader">
        <div class="wrap">
            <h1>Notre Equipe</h1>
        </div>
    </div>
    <div class="wrap">
        <section class="content equipe">
            <p>Lorem ipsum dolor sit amet, consectetur adipisicing elit. Quisquam, sunt. Lorem ipsum dolor sit amet, consectetur adipisicing elit. Ab aliquid amet culpa cupiditate dignissimos doloremque esse et eveniet id natus nihil provident quasi qui, quisquam quod repellat repudiandae ut voluptate!</p>
            <div class="quote">
                <p>Les détails font la perfection, et la perfection n'est pas un détail...</p>
                <img src="{{ asset('assets/images/testimonials_bg.png') }}" alt="quote"/>
                <span>de Vinci Leonard</span>
            </div>
            <div class="flex-container">
                {% for user in users %}
                {% set userImage = 'assets/images/'~user.photo %}
                    <div class="member">
                        <img src="{{ asset(userImage) }}" alt="{{ user.nom }} {{ user.prenom }}">
                        <h2>{{ user.nom }} {{ user.prenom }}</h2>
                        <h3>{{ user.function }}</h3>
                        <span>{{ user.diplome }}</span>
                    </div>
                {% endfor %}
            </div>
        </section>
    </div>
{% endblock %}


